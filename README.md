# Local Kanban Board

Simple local-first Kanban board built with Streamlit and SQLite.

Quick start

```bash
python -m pip install -r requirements.txt
streamlit run app.py
```

Database: `kanban_board.db` will be created in the project directory.
# kanban
Easy kanban app for project tracking
